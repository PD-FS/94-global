<%= form_with(model: report_class, local: true) do |form| %>
  <% if report_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report_class.errors.count, "error") %> prohibited this report_class from being saved:</h2>

      <ul>
      <% report_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :icon %>
    <%= form.text_field :icon %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
